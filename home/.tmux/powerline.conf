# Powerline-dependent extras — only sourced when the powerline binary exists.
# Static theming (colors, window formats, status-right) lives in theme.conf.

# Prefer powerline-client if present, else plain powerline
if-shell 'test -z "$POWERLINE_COMMAND"' 'if-shell "which powerline-client" "set-environment -g POWERLINE_COMMAND powerline-client" "set-environment -g POWERLINE_COMMAND powerline"'

# Append powerline segments after the session name
set -g status-left '#[fg=colour16,bg=white,bold] #S #[fg=white,bg=colour233,nobold] #(eval $POWERLINE_COMMAND tmux left)'
